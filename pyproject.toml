[project]
name = "autoclearance"
version = "0.1.0"
description = "autoclearance using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.8.0"
]

[project.scripts]
autoclearance = "autoclearance.main:run"
run_crew = "autoclearance.main:run"
train = "autoclearance.main:train"
replay = "autoclearance.main:replay"
test = "autoclearance.main:test"
run_with_trigger = "autoclearance.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
